#include<iostream>  // header file library for input and output objects such as cout
using namespace std; // we can use names for objects and variables from the standard library

int main() {
    // cout is an object used together with insertion operator (<<) to output/print text
    cout << "Hello World!";
    return 0;
}